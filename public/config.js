window.__NAV_CONFIG__ = {
  // Keep empty for same-origin reverse routing. For direct Pages + Worker deployments,
  // set this to your Worker URL, for example: "https://37-nav-api.your-subdomain.workers.dev".
  apiBase: ""
};
